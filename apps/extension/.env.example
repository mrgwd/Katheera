# Copy to .env and fill in real values. Never commit .env.
# Dev-only: consumed by DevAudioDebugger, which renders only in DEV builds,
# so this key is never inlined into production extension bundles.

VITE_EDGE_IMPULSE_API_KEY=ei_your_ingestion_api_key
